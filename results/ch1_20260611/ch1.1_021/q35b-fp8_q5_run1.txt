Here's a thinking process:

1.  **Understand User Requirements:**
   - Implement a thread-safe LRU cache in Python
   - Support `get(key)` and `put(key, value)` operations
   - Fixed capacity
   - Use only standard library
   - Include docstrings
   - Include pytest unit tests covering:
     - Eviction
     - Update
     - Concurrency

2.  **Core LRU Cache Design:**
   - Need a data structure that supports O(1) get and put operations
   - Standard approach: Doubly linked list + Hash map (dictionary)
   - Doubly linked list maintains order (most recent at head, least recent at tail)
   - Hash map maps keys to nodes in the linked list for O(1) lookup
   - Thread safety: Use `threading.Lock` to protect shared state
   - Fixed capacity: When full, evict the least recently used item (tail of the list)

3.  **Implementation Details (Standard Library Only):**
   - `collections.OrderedDict` could be used, but it's not thread-safe by itself. I could wrap it with a lock, but implementing a custom doubly linked list is more educational and gives full control. However, using `OrderedDict` with a lock is simpler and still standard library. Let's stick to the classic doubly linked list + dict approach for clarity and to demonstrate proper LRU implementation. Actually, Python's `collections.OrderedDict` is fine, but I'll implement the linked list explicitly to show the algorithm clearly. Wait, the prompt says "Use only the standard library", which both approaches satisfy. I'll go with the explicit doubly linked list for better understanding and control over thread safety.
   - Actually, using `collections.OrderedDict` with a `threading.Lock` is very Pythonic and efficient. But I'll implement the linked list approach as it's the standard algorithmic answer for LRU cache interviews/problems. Let's do the linked list + dict approach.
   - Node class: `key`, `value`, `prev`, `next`
   - LRU class: `__init__(capacity)`, `get(key)`, `put(key, value)`, `_add_node(node)`, `_remove_node(node)`, `_move_to_head(node)`, `_pop_tail()`
   - Thread safety: `self._lock = threading.Lock()` around `get` and `put` operations.

4.  **Draft Implementation (Mental):**
   ```python
   import threading

   class _Node:
       def __init__(self, key=None, value=None):
           self.key = key
           self.value = value
           self.prev = None
           self.next = None

   class LRUCache:
       def __init__(self, capacity: int):
           if capacity <= 0:
               raise ValueError("Capacity must be positive")
           self.capacity = capacity
           self.cache = {}  # key -> Node
           self._lock = threading.Lock()

           # Dummy head and tail for easier list management
           self.head = _Node()
           self.tail = _Node()
           self.head.next = self.tail
           self.tail.prev = self.head

       def _add_to_head(self, node):
           node.prev = self.head
           node.next = self.head.next
           self.head.next.prev = node
           self.head.next = node

       def _remove_node(self, node):
           prev_node = node.prev
           next_node = node.next
           prev_node.next = next_node
           next_node.prev = prev_node

       def _move_to_head(self, node):
           self._remove_node(node)
           self._add_to_head(node)

       def _pop_tail(self):
           node = self.tail.prev
           self._remove_node(node)
           return node

       def get(self, key):
           with self._lock:
               if key in self.cache:
                   node = self.cache[key]
                   self._move_to_head(node)
                   return node.value
               return -1

       def put(self, key, value):
           with self._lock:
               if key in self.cache:
                   node = self.cache[key]
                   node.value = value
                   self._move_to_head(node)
               else:
                   if len(self.cache) >= self.capacity:
                       tail_node = self._pop_tail()
                       del self.cache[tail_node.key]
                   new_node = _Node(key, value)
                   self.cache[key] = new_node
                   self._add_to_head(new_node)
   ```

   Wait, `get` returning `-1` is standard for LeetCode, but maybe returning `None` or raising `KeyError` is better? The prompt doesn't specify. I